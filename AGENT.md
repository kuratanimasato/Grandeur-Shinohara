# AGENTS.md

## Git Branch Rules

## リポジトリURL

- https://github.com/kuratanimasato/Apartment-site.git

### master

- 本番用ブランチ。
- 直接編集・直接コミットしない。
- 未検証のコードを入れない。
- fix/mainで動作確認が完了してからマージする。

### fix/main

- 開発・修正・検証用ブランチ。
- すべての修正は原則としてfix/mainで行う。
- GitHub Copilotによるコード変更もfix/mainで行う。
- 修正後は必ず動作確認する。

## Merge Rules

fix/mainからmasterへのマージは、以下をすべて確認してから行う。

1. PHPエラーがない
2. JavaScriptエラーがない
3. PC表示を確認
4. スマートフォン表示を確認
5. 修正箇所が意図通り動作する
6. 既存機能が壊れていない
7. WordPress管理画面が正常に動作する

動作確認が完了していない場合、masterへマージしない。

## WordPress Rules

- WordPressの標準的な設計・コーディング規約を優先する。
- 出力値は適切にエスケープする。
- ユーザー入力は適切にサニタイズする。
- CSS、JavaScript、画像などのアセットは既存のディレクトリ構成に従う。
- 既存コードを変更する前に、関連ファイルを確認する。
- 不要なファイルやライブラリを追加しない。

## AI Development Rules

- 既存コードを理解してから変更する。
- 大規模な変更を一度に行わない。
- 変更範囲を必要最小限にする。
- 変更後は関連するコードへの影響を確認する。
- 不明な仕様を勝手に決めない。
- 動作確認を行っていない変更を「完成」と判断しない。
